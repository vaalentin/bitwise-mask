import test from 'tape';

const Directions = {
  TOP: 1 << 0,
  RIGHT: 1 << 1,
  BOTTOM: 1 << 2,
  LEFT: 1 << 3
}

test('should be able to add and remove fields', t => {
});

test('should be able to combine fields', t => {
})

test('should be able to check if a mask contains a field', t => {
});

